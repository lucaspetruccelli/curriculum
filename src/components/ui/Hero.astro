---
import { Image } from 'astro:assets'

import SocialIcons from './SocialIcons.astro'


const {fullName, title, institute, profilePicture} = Astro.props
---
<div class='flex-1'>
  <h1 class='text-4xl font-bold mb-4'>
    Welcome! I'm <span class='text-secondary'>{fullName}</span>
  </h1>
  <p class='text-xl text-gray-600 mb-4'>{title}{institute ? ` - ${institute}` : ''}</p>
  <p class='text-lg mb-6'>
    En busca de oportunidades que me permitan desarrollarhabilidades y competencias. 
    Me considero un profesional con alto nivel de autoexigencia y compromiso con la excelencia, 
    con enfoque emprendedor para desarrollar oportunidades, proactivo y creativo ante la resolución deproblemas con ideas innovadoras.
  </p>
  <div class='flex gap-4'>
    <SocialIcons />
  </div>
</div>
<div class='size-72 lg:block hidden'>
  <!-- Add your profile image -->
  <Image
    src={profilePicture}
    alt={fullName}
    class='w-full h-full mask mask-circle size-44'
  />
</div>
